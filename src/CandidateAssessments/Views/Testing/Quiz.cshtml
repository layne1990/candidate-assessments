@{
    Layout = "_TestingLayout";
}
@model CandidateAssessments.Models.QuizQuestion

<script>
    var dt = '@ViewBag.EndDate';
    var dateAr = dt.split('-');
    var newDate = dateAr[1] + '/' + dateAr[0] + '/' + dateAr[2];

    var end = new Date(dateAr[1] + '/' + dateAr[0] + '/' + dateAr[2]);

    var _second = 1000;
    var _minute = _second * 60;
    var _hour = _minute * 60;

    var timer;

    function showRemaining() {
        var now = new Date();
        var distance = end - now;
        var limit = @ViewBag.TimeLimit;
        if (distance < ((limit * 60000) / 2)) {

            document.getElementById('countdown').className = "text-danger"

        }
        if (distance < 0) {
            clearInterval(timer);
            document.getElementById('countdown').innerHTML = 'EXPIRED';
            window.location.href = '/testing/assessment';

            return;
        }

        var minutes = Math.floor((distance % _hour) / _minute);
        var seconds = Math.floor((distance % _minute) / _second);


        document.getElementById('countdown').innerHTML = minutes + ':';
        if (seconds < 10)
            document.getElementById('countdown').innerHTML += '0' + seconds;
        else
            document.getElementById('countdown').innerHTML += seconds + '';
    }

    timer = setInterval(showRemaining, 1000);

</script>
<div class="container-fluid">

    <div class="row">
        <h4 class="col-md-offset-9">Time Remaining: <span id="countdown" class="text-success"></span></h4>
    </div>
    <form asp-action="Quiz">
        <input type="hidden" name="QuizQuestionId" value="@Model.QuizQuestionId"/>
        <div class="form-horizontal">
            <div class="form-group">

                <div class="card">
                    <h3 class="card-header">
                        <span class="cardHeader">Topic Goes Here</span><span class="pull-right">Question # @Html.DisplayFor(model => model.QuestionNumber)</span>
                    </h3>
                    <div class="card-block">
                        <blockquote class="card-blockquote">
                            <p class="card-text">@Html.DisplayFor(model => model.Question.QuestionText)</p>
                            <div class="list-group">
                                <label class="list-group-item" for="radio-1">
                                    <input type="radio" name="Answer" value="A" id="radio-1"/>
                                    <span class="bold">A.) </span>@Html.DisplayFor(model => model.Question.ChoiceA)
                                </label>
                                <label class="list-group-item" for="radio-1">
                                    <input type="radio" name="Answer" value="B" id="radio-1"/>
                                    <span class="bold">B.) </span>@Html.DisplayFor(model => model.Question.ChoiceB)
                                </label>
                                <label class="list-group-item" for="radio-1">
                                    <input type="radio" name="Answer" value="C" id="radio-1"/>
                                    <span class="bold">C.) </span>@Html.DisplayFor(model => model.Question.ChoiceC)
                                </label>
                                <label class="list-group-item" for="radio-1">
                                    <input type="radio" name="Answer" value="D" id="radio-1"/>
                                    <span class="bold">D.) </span>@Html.DisplayFor(model => model.Question.ChoiceD)
                                </label>
                            </div>
                        </blockquote>
                    </div>
                    <div class="dl-horizontal card-footer">
                        <span class="col-lg-1">
                        &nbsp;
                    </span>
                        <span class="col-lg-offset-10 col-lg-1">
                        <input type="submit" display="inline-block" class="btn btn-default" value="Next" />
                    </span>

                    </div>
                </div>
            </div>
        </div>
    </form>
</div>