@model CandidateAssessments.Models.Topic

@{
    ViewBag.Title = "New Topic";
}

<h2>@ViewData["Title"]</h2>

@using (Html.BeginForm())
{   
    @Html.AntiForgeryToken()

    <fieldset>
        <legend>Topic</legend>

        <div class="editor-label">
            @Html.LabelFor(topic => topic.Name)
        </div>
        <div class="editor-field">
            @Html.TextBoxFor(topic => topic.Name)
        </div>

        <input type="submit" value="Create">

    </fieldset>
}
<!--form asp-controller="Topic" asp-action="Create" method="post" class="form-horizontal" role="form">
    <div class="form-horizontal">
        <div asp-validation-summary="ValidationSummary.All" class="text-danger"></div>
        <div class="form-group">
            <label asp-for="Name" class="col-md-2 control-label"></label>
            <div class="col-md-10">
                <input asp-for="Name" class="form-control" />
                <span asp-validation-for="Name" class="text-danger"></span>
            </div>
        </div>
        <div class="form-group">
            <div class="col-md-offset-2 col-md-10">
                <input type="submit" value="Create" class="btn btn-default" />
            </div>
        </div>
    </div>
</form-->