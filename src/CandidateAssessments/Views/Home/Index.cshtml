
<br />
<div class="row">
    @if (User.IsInRole("Admin") || User.IsInRole("Recruiter")) { 
    <div class="col-md-12">
        <div class="jumbotron text-center">
            <h1>RDA Candidate Assessment</h1>
            <p>This is a tool for assessing potential hire's knowledge of languages and technologies.</p>

            @if (User.IsInRole("Admin") || User.IsInRole("Recruiter")) {
                <h2>Assessments</h2>
                <p>Create or review candidate assessments</p>
                <p><a class="btn btn-primary btn-lg" href="/Assessments" role="button">Let's Go!</a></p>
            }

            @if (User.IsInRole("Admin") || User.IsInRole("QuestionManager"))
            {
            <h2>Questions</h2>
            <p>Manage the topics and the pools of questions for each topic.</p>
            <p><a class="btn btn-primary btn-lg" href="/Topic" role="button">Let's Go!</a></p>
            }

        </div>
    </div>
    }
</div>
